# Local model outage

Local mode accepts only loopback model endpoints. A provider timeout or malformed response is a degraded local capability, never permission to contact a hosted endpoint. Continue with lexical retrieval or binary metadata extraction and retry the local provider explicitly.
