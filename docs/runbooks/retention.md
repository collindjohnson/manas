# Retention and purge

Deletes first become recoverable tombstones with deletion timestamps. Permanent purge requires the configured retention period, an administrative scope, explicit confirmation, a fresh target resolution, and an audit receipt.
